package ca.ucalgary.seng300.core.registry;

/**
 * Manages the catalog of available games on the platform.
 *
 * <p>Responsible for registering, unregistering, and listing games.
 * Implements the game registry interface from the shared contracts layer.
 * The data layer is owned by Platform Core; the UI flow is owned by
 * Client/UI in the screens package.</p>
 *
 * <p>TODO: Implement game registry operations (register, unregister, list,
 * search by tag) using the PersistenceService from shared interfaces.</p>
 */
public class GameRegistry {
    // TODO: Implement game registry — placeholder for Platform Core
}
